# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Tauri updater signing keys — NEVER commit
*.key
*.key.pub
signing.key*

# Env / secrets — NEVER commit (public repo). .env.example is the only one allowed.
.env
.env.*
!.env.example

# Local planning docs — not for the public repo
PLAN.md
UI_PLAN.md
IMPROVEMENT_PLAN.md
